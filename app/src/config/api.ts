import Constants from 'expo-constants';
import { Platform } from 'react-native';

// Always use deployed Render backend for all platforms
export const API_URL = 'https://emergencyresponse-n8cc.onrender.com/api';
export default API_URL;
